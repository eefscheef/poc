{
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/example-app",
    "packages/mcp-server"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "mutate": "npm run mutate --workspaces --if-present"
  }
}
